import React from "react";
import { HeaderMain } from "@/components/HeaderMain";

const Resume = () => (
  <article id="resume">
    <HeaderMain title="Resume" />
    <h2>Updating...</h2>
  </article>
);

export default Resume;
